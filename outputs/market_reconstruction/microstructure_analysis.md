# Microstructure and spread-proxy analysis

The bid-only M1 proxy contains 423 aligned entries; 0.641 have recorded price inside the matching reference-minute range. Buy/Sell reference-range position differs by Mann–Whitney p=0.8061 (n=214/209) if test else unavailable. This cannot estimate broker spread, ask price, slippage, or tick sequence.

The available source is an external bid-only M1 aggregate. The data cannot distinguish bid from ask, measure live spread, see intrabar jumps, or recover broker execution/slippage. Any remaining selection attributable to those variables is unidentifiable here.
